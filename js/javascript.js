

function validaciondatos() {
    let nombre = document.getElementById("name").value /* Se guarda en esta vairable el valor que viene en la propiedad nombre */
    alert("Bienvenido al formulario " + nombre)
}